DATABASE_URL=postgres://username:password@localhost/clublit_db
JWT_SECRET=enter_your_jwt_secret_here
GOOGLE_BOOKS_API_KEY=enter_your_google_books_api_key_here

Replace `<username>` and `<password>` with your PostgreSQL credentials. The `JWT_SECRET` can be any random string — it is used to sign and verify the JWT tokens for authentication.

For the `GOOGLE_BOOKS_API_KEY`, follow these steps to obtain one:

1. Visit the [Google Developers Console](https://console.developers.google.com/).
2. Create a new project or select an existing one.
3. Navigate to "Credentials" and then "Create credentials".
4. Select "API key" and follow the prompts to generate a new API key.